replicaCount: 2

image:
  repository: localhost:32000/cuegrowth-api
  tag: v1
  pullPolicy: IfNotPresent

service:
  port: 8000

ingress:
  enabled: true
  host: api.local

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "200m"
